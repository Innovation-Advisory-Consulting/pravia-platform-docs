# Database Migrations

## Overview
TypeORM migration files for schema changes.

## Usage
```bash
pnpm migration:generate
pnpm migration:run
pnpm migration:revert
```

See parent: `api/foundry/src/database/DATABASE.md`
