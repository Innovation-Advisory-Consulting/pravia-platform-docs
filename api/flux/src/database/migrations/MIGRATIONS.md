# Database Migrations

## Overview
TypeORM migration files for schema changes.

## Purpose
Version-controlled database schema changes.

## Usage
```bash
# Generate migration
pnpm migration:generate

# Run migrations
pnpm migration:run

# Revert migration
pnpm migration:revert
```

See parent: `api/flux/src/database/DATABASE.md`
